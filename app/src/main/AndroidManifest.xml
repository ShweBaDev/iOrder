<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.galaxysoftware.istockenterpriseiorder">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IOrder"
        android:usesCleartextTraffic="true">
        <activity
            android:screenOrientation="portrait"
            android:name=".views.ui.checkout.CheckoutActivity"
            android:label="Check Out"
            ></activity>
        <activity
            android:screenOrientation="portrait"
            android:name=".views.ui.itemdetail.ItemDetailActivity"
            android:label="@string/ItemDetail" />
        <activity
            android:screenOrientation="portrait"
            android:name=".views.MainConsoleActivity"
            android:label="@string/title_activity_main_console"
            android:theme="@style/Theme.IOrder.NoActionBar" />
        <activity android:name=".views.SignInActivity" />
        <activity
            android:screenOrientation="portrait"
            android:name=".MainActivity" />
        <activity
            android:name=".SplashScreenActivity"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>